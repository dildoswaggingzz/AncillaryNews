# Auto-provisions every dashboard JSON in ../../dashboards -- no manual
# clicking required.
apiVersion: 1

providers:
  - name: AncillaryNews
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
